---
title: "ReportBrowserErrorOptions"
description: ""
---

<RequestExample>

```typescript Interface lines
export interface ReportBrowserErrorOptions {
  /**
   * The error object from the WebAuthn API. This typically is a `DOMException`.
   * The SDK expects an object with at least `name` and `message` properties.
   * @type {WebAuthnErrorDetails}
   * @example { name: "NotAllowedError", message: "The operation was cancelled by the user." }
   */
  error: WebAuthnErrorDetails;
}
```

</RequestExample>

## Properties

<ParamField path="error" type={<span><a href="/docs/customize/login-pages/advanced-customizations/reference/js-sdk/interfaces/WebAuthnErrorDetails">WebAuthnErrorDetails</a></span>} required>
  The error object from the WebAuthn API. This typically is a `DOMException`. The SDK expects an object with at least `name` and `message` properties.
</ParamField>

---

**File:** [interfaces/screens/reset-password-mfa-webauthn-platform-challenge.ts](https://github.com/auth0/universal-login/blob/master/packages/auth0-acul-js/interfaces/screens/reset-password-mfa-webauthn-platform-challenge.ts)
