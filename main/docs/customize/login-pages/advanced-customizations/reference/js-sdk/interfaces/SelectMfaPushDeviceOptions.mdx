---
title: "SelectMfaPushDeviceOptions"
description: "Options for selecting an MFA push device."
---

<RequestExample>

```typescript Interface lines
export interface SelectMfaPushDeviceOptions {
  /**
   * The index of the device to select from the list of enrolled devices.
   * The index is 0-based.
   */
  deviceIndex: number;
  [key: string]: string | number | boolean | undefined;
}
```

</RequestExample>

## Properties

<ParamField path="deviceIndex" type={<span>number</span>} required>
  The index of the device to select from the list of enrolled devices. The index is 0-based.
</ParamField>

<ParamField path="unknown" type={<span>string | number | boolean | undefined</span>}>
</ParamField>

---

**File:** [interfaces/screens/mfa-push-list.ts](https://github.com/auth0/universal-login/blob/master/packages/auth0-acul-js/interfaces/screens/mfa-push-list.ts)
