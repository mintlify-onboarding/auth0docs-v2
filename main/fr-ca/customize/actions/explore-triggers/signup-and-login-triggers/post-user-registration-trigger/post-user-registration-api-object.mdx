---
title: "Actions - Déclencheurs - post-user-registration - Objet API"
permalink: "post-user-registration-api-object"
'description': "Découvrez l’objet API du déclencheur d’action post-user-registration."
'og:title': "Actions - Déclencheurs - post-user-registration - Objet API"
'og:description': "Découvrez l’objet API du déclencheur d’action post-user-registration."
'og:image': "https://cdn2.auth0.com/docs/1.14567.0/img/share-image.png"
'twitter:title': "Actions - Déclencheurs - post-user-registration - Objet API"
'twitter:description': "Découvrez l’objet API du déclencheur d’action post-user-registration."
---

L’objet API pour le déclencheur d’actions post-user-registration inclut :

### api.cache

Stockez et récupérez les données qui persistent dans les exécutions.

### api.cache.delete(key)

Supprime un enregistrement décrivant une valeur mise en cache à la clé fournie, si elle existe.

Renvoie un objet `CacheWriteResult` de `type: "success"` si une valeur a été retirée de la mémoire cache. Une opération échouée renvoie le `type: "error"`. Pour les erreurs, l’objet renvoyé aura une propriété `code` qui indique la nature de l’échec.

### api.cache.get(key)

Récupérez un enregistrement qui décrit une valeur mise en cache à la `key` fournie, le cas échéant. Si un enregistrement est trouvé, la valeur mise en cache se trouve dans la propriété `value` de l’objet retourné.

Renvoie un enregistrement mis en cache si un élément est trouvé dans la mémoire cache pour la `key` fournie. Les enregistrements de cache sont des objets dotés d’une propriété `value` contenant la valeur mise en cache et d’une propriété `expires_at` indiquant l’expiration maximale de l’enregistrement en millisecondes depuis l’époque Unix.

**Important :** Ce cache est conçu pour les données éphémères à vie courte. Les éléments peuvent ne pas être disponibles dans les transactions ultérieures, même s’ils sont encore dans leur durée de vie.

### api.cache.set(key, value, [options])

Stocke ou met à jour une valeur de chaîne dans le cache à la clé spécifiée.

Les valeurs stockées dans ce cache sont limitées au déclencheur dans lequel elles sont définies. Elles sont assujetties aux [limites du cache des actions](https://auth0.com/docs/customize/actions/limitations).

Les valeurs stockées ainsi auront des durées de vie au plus équivalentes aux valeurs `ttl` ou `expires_at` fournies. Si aucune durée de vie n’est précisée, une durée de vie par défaut de 15 minutes sera utilisée. Les durées de vie ne peuvent pas dépasser la durée maximale énoncée à la page [Limites du cache des actions](https://auth0.com/docs/customize/actions/limitations).